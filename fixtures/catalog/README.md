# Fixture catalog

A minimal `.agents` tree holding only `workflows/<slug>/workflow.yaml` files,
copied from [`ai-outfitter/community-profiles`](https://github.com/ai-outfitter/community-profiles)
so the app and its tests run without a checkout of the real catalog.
Point `catalogs:` in `a2astro.config.yaml` at a real catalog for production.
